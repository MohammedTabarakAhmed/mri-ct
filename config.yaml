data:
  image_size: 256
  batch_size: 8
  num_workers: 2

training:
  epochs: 10
  lr: 0.0001

model:
  encoder: resnet34
  pretrained: true

paths:
  train_images: data/processed/images
  train_masks: data/processed/masks
  model_save: outputs/models/model.pth